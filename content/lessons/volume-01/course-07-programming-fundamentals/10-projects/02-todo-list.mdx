---
title: "Project: Todo List"
slug: project-todo-list
module: "Projects"
moduleOrder: 10
lessonOrder: 2
duration: 30
xp: 65
difficulty: intermediate
concepts: [functions, arrays, control-flow]
prerequisites: [function-challenge, for-loops]
learningObjectives:
  - Build a functional todo list application
  - Manage state with arrays and functions
estimatedKnowledgeGain: 9
tags: [project, todo-list, arrays, functions]
---

## 1. Project Brief

Build a todo list manager that runs in the console. Users can add, list, complete, and delete todos.

## 2. Requirements

1. `add(todo)` — adds a new todo
2. `list()` — shows all todos with their status
3. `complete(index)` — marks a todo as done
4. `remove(index)` — removes a todo
5. `summary()` — shows counts (total, done, pending)

## 3. Starter Code

<Terminal mode="challenge" language="javascript" initialCode="// Todo List Manager
let todos = [];

function add(todo) {
  // Your code here
}

function list() {
  // Your code here
}

function complete(index) {
  // Your code here
}

function remove(index) {
  // Your code here
}

function summary() {
  // Your code here
}

// Test your functions:
add(&quot;Learn JavaScript&quot;);
add(&quot;Build a project&quot;);
add(&quot;Review lessons&quot;);
list();
complete(0);
summary();" />

## 4. Solution Approach

```javascript
let todos = [];

function add(todo) {
  todos.push({ text: todo, done: false });
}

function list() {
  todos.forEach((todo, i) => {
    let status = todo.done ? "[✓]" : "[ ]";
    console.log(i + ": " + status + " " + todo.text);
  });
}

function complete(index) {
  if (todos[index]) todos[index].done = true;
}

function remove(index) {
  todos.splice(index, 1);
}
```

## 5. Summary

- Real applications manage state with arrays
- Functions encapsulate operations
- Objects group related data (text + status)
- Build features incrementally
